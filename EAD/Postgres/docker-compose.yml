version: "3.2"
services:
  
  postgres:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=3301
    ports:
      - 5432:5432
    volumes:
      - ./db:/docker-entrypoint-initdb.d